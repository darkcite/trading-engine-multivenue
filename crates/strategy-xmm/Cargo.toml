[package]
name         = "strategy-xmm"
version      = "0.1.0"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
publish.workspace      = true
authors.workspace      = true
description  = "XMM slot-6 member: a Binance-led post-only market maker on Hyperliquid perps (policy LEAD θ), paper-first (O-XH1)."

[lib]
path = "src/lib.rs"

[dependencies]
core-types    = { workspace = true }
strategy-core = { workspace = true }

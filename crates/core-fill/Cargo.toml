[package]
name         = "core-fill"
version      = "0.1.0"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
publish.workspace      = true
authors.workspace      = true
description  = "The ONE modelled fill law (VRP X1): IoC judged once at the touch, makers on a strict cross, shared displayed-size budgets, the I1 TTL. Consumed by the offline harness and by the engine's paper dispatcher so a paper position and its replayed fill cannot disagree."

[lib]
path = "src/lib.rs"

[dependencies]
core-types = { workspace = true }

[package]
name         = "strategy-core"
version      = "0.1.0"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
publish.workspace      = true
authors.workspace      = true
description  = "The `Strategy` trait every strategy crate implements. Contract is compile-time monomorphized — no dyn Trait anywhere."

[lib]
path = "src/lib.rs"

[dependencies]
core-types = { workspace = true }
core-time  = { workspace = true }

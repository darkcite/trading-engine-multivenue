[package]
name         = "ingress-bybit"
version      = "0.1.0"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
publish.workspace      = true
authors.workspace      = true
description  = "Bybit v5 public WebSocket ingress (WS9): orderbook.1 BBO / publicTrade / tickers (linear mark-index-funding-OI), spot + linear connections on one thread. Byte-scanner parsers, zero-alloc steady state."

[lib]
path = "src/lib.rs"

[dependencies]
core-types   = { workspace = true }
core-parse   = { workspace = true }
core-net     = { workspace = true }
core-ring    = { workspace = true }
core-metrics = { workspace = true }
core-time    = { workspace = true }
memchr       = { workspace = true }
mio          = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }

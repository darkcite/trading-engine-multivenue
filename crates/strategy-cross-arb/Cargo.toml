[package]
name         = "strategy-cross-arb"
version      = "0.1.0"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
publish.workspace      = true
authors.workspace      = true
description  = "Strategy C: cross-market arbitrage. Detects sum-of-probabilities violations across linked Polymarket markets and emits N synthetic hedge orders per group."

[lib]
path = "src/lib.rs"

[dependencies]
strategy-core = { workspace = true }
core-types    = { workspace = true }
core-time     = { workspace = true }
book-builder  = { workspace = true }

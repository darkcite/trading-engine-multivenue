[package]
name         = "tui"
version      = "0.1.0"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
publish.workspace      = true
authors.workspace      = true
description  = "Read-only ratatui dashboard over the engine's 1 s EngineSnapshot (the same POD GET /state serves) — never touches the hot path directly."

[lib]
path = "src/lib.rs"

[dependencies]
core-types      = { workspace = true }
engine-snapshot = { workspace = true }
ratatui         = { workspace = true }
crossterm       = { workspace = true }

[dev-dependencies]
strategy-core   = { workspace = true }

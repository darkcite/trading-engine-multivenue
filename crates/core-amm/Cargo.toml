[package]
name         = "core-amm"
version      = "0.1.0"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
publish.workspace      = true
authors.workspace      = true
description  = "HYPARB H1: concentrated- and constant-product AMM swap simulation, bit-exact with the Uniswap V3 contracts (tick math, bitmap-word step walk, exact fee arithmetic), and the single-block arb sizing solve. Zero-alloc, no dyn, no floats, no unsafe."

[lib]
path = "src/lib.rs"

[dev-dependencies]
proptest = { workspace = true }

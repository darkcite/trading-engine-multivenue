[package]
name         = "core-alloc"
version      = "0.1.0"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
publish.workspace      = true
authors.workspace      = true
description  = "Allocation counter / global allocator wrapper used ONLY in tests and benches. Asserting 0 B/op on hot paths."

[lib]
# Plain rlib, no proc-macros, no dylib — this crate is linked into tests.
path = "src/lib.rs"

[dependencies]
# No runtime deps. Uses std only.

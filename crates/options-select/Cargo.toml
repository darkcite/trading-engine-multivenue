[package]
name         = "options-select"
version      = "0.1.0"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
publish.workspace      = true
authors.workspace      = true
description  = "The M2 capped-options-universe selection LAW (nearest-E expiries × K nearest-ATM strikes, C-before-P, deterministic allocation order) — one home, generic over the per-venue discovery row types. Boot-only; zero dependencies."

[lib]
path = "src/lib.rs"

[dependencies]

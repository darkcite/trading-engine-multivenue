[package]
name         = "strategy-news"
version      = "0.1.0"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
publish.workspace      = true
authors.workspace      = true
description  = "News-driven probability shifts (RSS now, X/Benzinga later). Maps tagged news to affected markets via Claude-generated artifacts."

[lib]
path = "src/lib.rs"

[dependencies]
strategy-core = { workspace = true }
core-types    = { workspace = true }
core-time     = { workspace = true }
